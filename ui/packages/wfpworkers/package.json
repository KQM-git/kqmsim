{
  "name": "@gcsim/wfpworkers",
  "packageManager": "yarn@3.2.4",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "start": "wrangler dev",
    "upload:wasm": "wrangler r2 object put kqmsim/$(git rev-parse HEAD).wasm --file=../wfp/public/main.wasm",
    "deploy": "yarn upload:wasm && wrangler deploy",
    "version": "yarn upload:wasm && wrangler versions upload"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230115.0",
    "@types/pako": "^2.0.0",
    "typescript": "^4.8.4",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "@cfworker/json-schema": "^1.12.5",
    "@cfworker/uuid": "^1.12.4",
    "axios": "^1.6.7",
    "itty-router": "^3.0.11",
    "pako": "^2.1.0"
  }
}
